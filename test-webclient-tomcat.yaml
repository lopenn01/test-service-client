apiVersion: v1
kind: Service
metadata:
  name: test-webclient-tomcat
  labels:
    app.kubernetes.io/name: test-webclient-tomcat
    app.kubernetes.io/instance: test-webclient-tomcat
    app.kubernetes.io/version: "0.0.1"
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
  selector:
    app.kubernetes.io/name: test-webclient-tomcat
    app.kubernetes.io/instance: test-webclient-tomcat
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: test-webclient-tomcat
  labels:
    app.kubernetes.io/name: test-webclient-tomcat
    app.kubernetes.io/instance: test-webclient-tomcat
    app.kubernetes.io/version: "0.0.1"
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: test-webclient-tomcat
      app.kubernetes.io/instance: test-webclient-tomcat
  template:
    metadata:
      labels:
        app.kubernetes.io/name: test-webclient-tomcat
        app.kubernetes.io/instance: test-webclient-tomcat
    spec:
      containers:
        - name: test-webclient-tomcat
          image: "test-webclient-tomcat:0.0.1"
          imagePullPolicy: Never
          env:
          - name: ENV
            value: "local"
          resources:
            limits:
              cpu: 1000m
              memory: 0.32Gi
            requests:
              cpu: 250m
              memory: 0.32Gi
